[package]
name = "orchestrator"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
config = { path = "../config" }
downloader = { path = "../downloader" }
fetcher = { path = "../fetcher" }

tokio = { version = "1.43.0", features = ["time", "process", "io-util"] }
