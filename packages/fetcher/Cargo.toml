[package]
name = "fetcher"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
config = { path = "../config" }

rspotify = { version = "0.14.0", default-features = false, features = [
  "env-file",
  "client-ureq",
  "ureq-rustls-tls",
] }
rustypipe = "0.11.0"
futures = "0.3.31"
async-trait = "0.1.86"